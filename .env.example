# SemaphoreUI connection settings
SEMAPHORE_URL=http://localhost:3000
# Optional with MCP_TRANSPORT=http: the client's Authorization: Bearer header is forwarded instead
SEMAPHORE_API_TOKEN=

# MCP server settings
MCP_LOG_LEVEL=INFO
